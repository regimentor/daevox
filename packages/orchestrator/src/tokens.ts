const dialogMessagesStoreToken = Symbol("dialog-messages-store");
const completionFunctionToken = Symbol("completion-function");
const memoryGroomerConfigToken = Symbol("memory-groomer-config");
const memoryClientToken = Symbol("memory-client");

export {
  completionFunctionToken,
  dialogMessagesStoreToken,
  memoryClientToken,
  memoryGroomerConfigToken,
};
