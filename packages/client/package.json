{
  "name": "@daevox/client",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "test": "vitest run src/api.test.ts",
    "build": "tsc --build && vite build && vite build --mode preload",
    "start": "npm run build && electron dist/index.js"
  },
  "dependencies": {
    "@daevox/contracts": "^1.0.0",
    "@daevox/domain": "^1.0.0",
    "@daevox/ui": "^1.0.0",
    "async-mutex": "^0.5.0"
  },
  "devDependencies": {
    "electron": "^43.3.0",
    "vite": "^8.2.1",
    "vitest": "latest"
  }
}
